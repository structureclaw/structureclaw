# Intent

- 适用于规则楼层、规则跨布置的 2D 平面框架与 3D 规则轴网框架。
- 首轮 ready 以维度、层数、跨数、层高、跨长、各层总荷载为主。
- 材料牌号与梁柱截面优先提取，但不是阻塞项；缺失时可由系统推荐默认值后继续分析。
- 若没有任何 Y 向几何或荷载证据，默认按 2D 平面框架理解；一旦出现 Y 向证据则收敛到 3D。
- 若结构存在退台、缺跨或明显不规则，应改用 JSON 或更具体的节点/杆件描述。
