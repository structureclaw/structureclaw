{
  "name": "structureclaw-backend",
  "version": "1.0.0",
  "description": "StructureClaw Backend Service",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "test": "npm run db:generate && npm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests",
    "test:skills": "npm run db:generate && npm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests --runInBand src/agent-skills/",
    "test:skill:beam": "npm run db:generate && npm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests --runInBand src/agent-skills/structure-type/beam/",
    "test:skill:frame": "npm run db:generate && npm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests --runInBand src/agent-skills/structure-type/frame/",
    "test:skill:portal-frame": "npm run db:generate && npm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests --runInBand src/agent-skills/structure-type/portal-frame/",
    "db:generate": "prisma generate && node ./scripts/ensure-prisma-client-commonjs.mjs",
    "db:push": "npm run db:generate && node ./scripts/sync-sqlite-schema.mjs",
    "db:migrate": "npm run db:generate && node ./scripts/sync-sqlite-schema.mjs",
    "db:validate": "prisma validate",
    "db:deploy": "npm run db:generate && node ./scripts/sync-sqlite-schema.mjs",
    "db:seed": "prisma db seed",
    "db:init": "npm run db:deploy && prisma db seed",
    "db:import:postgres": "node ./scripts/migrate-postgres-to-sqlite.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/static": "^9.1.3",
    "@fastify/swagger": "^9.7.0",
    "@fastify/swagger-ui": "^5.2.6",
    "@langchain/core": "^1.1.40",
    "@langchain/langgraph": "^1.2.9",
    "@langchain/openai": "^1.4.4",
    "@prisma/client": "^6.19.2",
    "fastify": "^5.8.5",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "yaml": "^2.8.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "@types/pg": "^8.11.0",
    "@typescript-eslint/eslint-plugin": "^7.5.0",
    "@typescript-eslint/parser": "^7.5.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "pg": "^8.20.0",
    "prisma": "^6.19.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
