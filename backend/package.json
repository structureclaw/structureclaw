{
  "name": "structureclaw-backend",
  "version": "0.1.0",
  "description": "StructureClaw Backend Service",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "test": "npm run db:generate && npm run build && cross-env NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests",
    "db:generate": "prisma generate && node ./scripts/ensure-prisma-client-commonjs.mjs",
    "db:push": "npm run db:generate && node ./scripts/sync-sqlite-schema.mjs",
    "db:migrate": "npm run db:generate && node ./scripts/sync-sqlite-schema.mjs",
    "db:validate": "prisma validate",
    "db:deploy": "npm run db:generate && node ./scripts/sync-sqlite-schema.mjs",
    "db:seed": "prisma db seed",
    "db:init": "npm run db:deploy && prisma db seed",
    "db:import:postgres": "node ./scripts/migrate-postgres-to-sqlite.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/swagger": "^8.14.0",
    "@fastify/swagger-ui": "^4.0.0",
    "@langchain/openai": "^1.4.1",
    "@prisma/client": "^6.19.2",
    "dotenv": "^17.3.1",
    "fastify": "^4.26.2",
    "pg": "^8.20.0",
    "pino": "^8.19.0",
    "pino-pretty": "^11.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.12.2",
    "@typescript-eslint/eslint-plugin": "^7.5.0",
    "@typescript-eslint/parser": "^7.5.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "prisma": "^6.19.2",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
